// I was not able to understand that is this solution even correct or i might have understood it properly. and thanks to gfg comment section that gives the answer rather than discussing about the question

class Solution {
    void matchPairs(int n, char nuts[], char bolts[]) {
        Arrays.sort(nuts);
        Arrays.sort(bolts);
    }
}
